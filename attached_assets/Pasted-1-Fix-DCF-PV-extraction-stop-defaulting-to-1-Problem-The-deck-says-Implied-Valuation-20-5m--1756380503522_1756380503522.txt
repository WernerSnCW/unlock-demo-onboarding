1) Fix DCF PV extraction + stop defaulting to 1

Problem: The deck says “Implied Valuation ~£20,5m”. Your extractor didn’t normalise the comma decimal and the downstream default set value to 1.

Action:

Ensure we use the enhanced funding extractor and normalise “EU comma” decimals.

In valuation_engine.ts (or wherever your parse helpers live), replace your money parser with this (it handles £20,5m, ~£20.5m, £20m, £20,000,000, etc.):

// NORMALISES EU comma decimals + units + currency
export function parseMoney(raw: string): { value?: number; currency?: string } {
  if (!raw) return {};
  let s = raw.trim()
    .replace(/[~≈≃]/g, "")            // remove approx marks
    .replace(/\s+/g, " ");             // normalise spaces
  // turn 20,5m -> 20.5m  (but keep 20,000,000)
  s = s.replace(/(\d),(\d{1,2})(?=[^\d]|$)/g, "$1.$2");

  const currencyMatch = s.match(/(GBP|EUR|USD|US\$|£|\$|€)/i);
  const currency = currencyMatch ? currencyMatch[0].toUpperCase().replace(/[^\p{L}£$€]/gu, "") : undefined;

  const numMatch = s.match(/(\d{1,3}(?:[,\s]\d{3})+|\d+(?:[.,]\d+)?)/);
  if (!numMatch) return { currency };
  const num = parseFloat(numMatch[1].replace(/[,\s]/g, ""));
  const unitMatch = s.match(/\b(k|m|mn|bn)\b/i);
  const unit = unitMatch ? ({k:1e3,m:1e6,mn:1e6,bn:1e9} as any)[unitMatch[1].toLowerCase()] : 1;

  return { value: Number.isFinite(num) ? num * unit : undefined, currency };
}


In your LLM funding extractor prompt (enhanced extractor): make sure it asks for valuation_dcf_present and says return numbers, not strings; treat “£20,5m” as 20500000. (From your previous changes this is already there; keep it.)

In pitchDeckAnalyzer.ts when setting the headline value do NOT default to 1. Use null and let the UI print “Not specified”:

const declaredPV =
  extracted.kpis.valuation_dcf_present ??
  extracted.kpis.stated_pre_money ??
  extracted.kpis.stated_post_money ??
  null; // NOT 1

2) Compute “Gap vs peers” from PV mid vs deck PV

You already compute discounted PV midpoints. Now drive the gap off those and the deck PV (from step 1):

// basePV = PV midpoint from revenue_multiple or ebitda_multiple (your engine sets implied_mid)
const basePV = valuations.revenue_multiple?.implied_mid
            ?? valuations.ebitda_multiple?.implied_mid
            ?? null;

const deckPV = declaredPV; // from step 1

result.valuation.assessment =
  (deckPV && basePV)
    ? `Gap vs peers: ${Math.round(((deckPV - basePV) / basePV) * 100)}%`
    : "Gap vs peers: not enough data";

3) Label the base correctly and show horizon (don’t say “Current ARR”)

If you’re valuing on forecast ARR, label it as such.

In your valuation engine ensure you return:

results.revenue_multiple = {
  base,                // number (ARR if present else revenue)
  base_label: kpis.arr ? "ARR" : "Revenue",
  horizon_years: kpis.arr_horizon_years ?? 0,
  multiple_mid: ...,
  implied_mid: ...     // PV, already discounted
}


In the UI mapping (pitchDeckAnalyzer.ts):

revenueMultiple: {
  base: valuations.revenue_multiple?.base || 0,
  baseLabel: valuations.revenue_multiple?.base_label || "Revenue",
  horizonYears: valuations.revenue_multiple?.horizon_years ?? 0,
  multiple: valuations.revenue_multiple?.multiple_mid || 0,
  impliedValue: valuations.revenue_multiple?.implied_mid || 0,
}


In the component that prints “Current ARR:” render:

If horizonYears > 0: ${baseLabel} (Year ${horizonYears})

Else: Current ${baseLabel}

4) Kill the “£NaN ARR” string

In the UI wherever you print “Based on £… ARR”, use a safe formatter:

const base = valuation.methods.revenueMultiple.base;
const baseLabel = valuation.methods.revenueMultiple.baseLabel || "Revenue";
const baseText = Number.isFinite(base) ? money(base) : "not specified";
subtitle = `Based on ${baseText} ${baseLabel.toUpperCase()}`;


(money() is your usual currency formatter.)

This avoids “£NaN …”.

5) Keep ROI hidden when terms are missing (you’ve done this—double-check)

No terms in Unlock deck → ROI card must not render.

const hasTerms = !!(kpis.raise_amount && kpis.equity_offered_pct);
if (!hasTerms) delete valuation.methods.roiProjection;